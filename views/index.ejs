<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"> 
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans"> 
</head>
<body>

  <img src="/systech-logo-new.webp" class="img-fluid" alt="systech logo">
  <div class="tab-container">
    <div class="tab-buttons ">
      <button class="tab-button " id="tab1Button"  onclick="openTab('tab1')">Unisecure</button>
      <button class="tab-button " id="tab2Button"  onclick="openTab('tab2')">Unitrace</button>
      <button class="tab-button" id="tab3Button" onclick="openTab('tab3')">Uniseries</button>
      <button class="tab-button" id="tab4Button" onclick="openTab('tab4')">Forms & Archives</button>
      
      
      
    </div>
    <div class="tab-content" id="tab1">Tab 1 Content</div>
    <div class="tab-content" id="tab2">Tab 2 Content</div>
    <div class="tab-content" id="tab3">Tab 3 Content</div>
    <div class="tab-content" id="tab4">Tab 4 Content</div>
  </div>

  <!-- Repeat the same structure for the other tabs -->

  <script>
    function openTab(tabId) {
       // Remove the "active" class from all buttons
  var buttons = document.querySelectorAll('.tab-button');
  for (var i = 0; i < buttons.length; i++) {
    buttons[i].classList.remove('active');
  }

  // Add the "active" class to the clicked button
  var tabButton = document.getElementById(tabId + 'Button');
  if (tabButton) {
    tabButton.classList.add('active');
  }

      var tabContents = document.getElementsByClassName('tab-content');
      for (var i = 0; i < tabContents.length; i++) {
        tabContents[i].style.display = 'none';
      }
      
      // Use AJAX to load content from the external file
      var xhr = new XMLHttpRequest();
      xhr.open('GET', tabId, true);
      xhr.onreadystatechange = function() {
        if (xhr.readyState === 4 && xhr.status === 200) {
          document.getElementById(tabId).innerHTML = xhr.responseText;
          console.log(document.getElementById(tabId).innerHTML);
          document.getElementById(tabId).style.display = 'block';
          
        }
      };
      xhr.send();
    }

  //   function openForm() {
  //   // Use AJAX to load the form content
  //   var xhr = new XMLHttpRequest();
  //   xhr.open('GET', '/form', true);
  //   xhr.onreadystatechange = function () {
  //     if (xhr.readyState === 4) {
  //       if (xhr.status === 200) {
  //         // Display the form content in the formContainer
  //         document.getElementById('formContainer').innerHTML = xhr.responseText;
  //         document.getElementById('formContainer').style.display = 'block';
  //       } else {
  //         console.error('Error loading form. Status:', xhr.status);
  //       }
  //     }
  //   };
  //   xhr.send();
  // }
 
  function openForm() {
    // Open the formPage in a new window or tab
    window.open('/form', '_blank');
  }






  </script>

  <footer >
    <p style="font-size: 18px; font-weight: bold;">&copy; 2023 Systech</p>
        <p style="font-size: 14px;">All rights reserved.</p>
  </footer>
</body>
</html>